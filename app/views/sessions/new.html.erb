<h1>Sign in</h1>

<%= form_with url: session_path, method: :post, html: { class: "session-form" } do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>
  <%= f.submit "Sign in" %>
<% end %>
